declare const _default: (a?: any) => any;
/**
 * @API
 * 交换 fn 的头两个参数
 * 返回柯里化后的新函数
 *
 * 对比：
 * Rf.curry((a, b) => a - b)(1, 2) // -1
 * Rf.flip((a, b) => a - b)(1, 2) // 1
 *
 */
export default _default;
